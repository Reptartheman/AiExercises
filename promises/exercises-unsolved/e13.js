/* 
  Exercise 13: Validate and Format Input
Create a function validateAndFormatInput that:

Takes an input.
Returns a promise that:
First checks if the input is a string and rejects if it’s not.
If valid, chains a .then() to remove any leading and trailing whitespace from the input.
Chains another .then() to convert the trimmed string to lowercase.
Returns the formatted string.
*/

export const validateAndFormatInput = (input) => {

}
