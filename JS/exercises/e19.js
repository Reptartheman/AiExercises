/* 
Exercise 4: Normalize Effect Index
Write a function that takes a track object and an index, and returns the effect at the normalized index. If the effects array is empty, return null.
*/

import { tracks } from "../../music.js";



function normalizeEffectIndex(tracks, index) {

}




normalizeEffectIndex(tracks[3], -1);

/* 
"delay" // Last effect in the "Happy Hour" track
*/