/* 
Exercise 4: Create Scale Patterns
Write a function that takes the name of a major scale and the number of octaves, and returns the sequence of notes for the scale repeated over the given number of octaves.
*/

import { scales } from '../../music.js';


function createScalePattern(scaleName, interval, scales) {
  
}


createScalePattern('C-Major', 3, scales);


/* 
  Expected output
  ["C", "D", "E", "F", "G", "A", "B", "C", "D", "E", "F", "G", "A", "B", "C", "D", "E", "F", "G", "A", "B"]
*/