/* 
Exercise 16: Normalize Collaborator Index
Write a function that takes a track object (from the tracks array) 
and an index, and returns the collaborator at the normalized index. 
If the collaborators array is empty, return null.
*/

import { tracks } from '../../music.js';


function normalizeCollaboratorIndex(arrayIndex, number) {
  
}



normalizeCollaboratorIndex(tracks[0], 5);

