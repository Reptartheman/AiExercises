/* 
Exercise 17: Normalize Scale Note Index
Write a function that takes a scale name, an index, and the scales object, and returns the note at the normalized index. If the scale doesn’t exist, return undefined.
*/

import { scales } from "../../music.js"

function normalizeScaleNoteIndex(scaleName, index, scales) {

}



normalizeScaleNoteIndex('C-Major', 10, scales);


/* 
"E" // Loops back through "C, D, E, F, G, A, B"

*/