/* Exercise 3: Filter Strings by Length
Task: Write a function that takes the arrayOfStuff and returns all strings longer than 5 characters.

Input: arrayOfStuff array.
Output: Array of strings longer than 5 characters. */


import { arrayOfStuff } from "../practice.js";


const filterStrings = (array) => {
  console.log(array)
}

console.log(filterStrings(arrayOfStuff));